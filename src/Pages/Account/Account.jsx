import React from "react";
import AccountDetails from "./AccountDetails"; // Import the Account component

const Account = () => {

  return (
    <div className="account-container" style={{backgroundColor: "#ffff"}}>
      <AccountDetails/>
    </div>
  );
};

export default Account;
